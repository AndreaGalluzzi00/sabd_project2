extends: "../base.yml"

# Sweep parallelismo — vedi 07_par1.yml per la motivazione. Parallelismo 2.

experiment:
  name: "08_par2"
  output_dir_host: "Results/experiments"

producer:
  holdback_probability: 0.0

flink:
  parallelism: 2
