extends: "../base.yml"

experiment:
  name: "baseline"
  output_dir_host: "Results/experiments"

producer:
  holdback_probability: 0.0


q1:
  watermark_delay_seconds: 30
q2:
  window: "all"   # 1h | 6h | global | all | cumulative
  watermark_delay_seconds: 30
q3:
  window: "all"  # 1d | 7d | global | all  cumulative
  watermark_delay_seconds: 30
