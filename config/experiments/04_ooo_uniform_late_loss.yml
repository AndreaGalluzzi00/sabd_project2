extends: "../base.yml"

experiment:
  name: "04_ooo_uniform_late_loss"
  output_dir_host: "Results/experiments"

producer:
  holdback_probability: 0.3
  holdback_delay: 120.0
  holdback_distribution: "uniform"

q1:
  watermark_delay_seconds: 30
