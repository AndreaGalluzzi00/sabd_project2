extends: "../base.yml"

# Sweep parallelismo — vedi 07_par1.yml per la motivazione. Parallelismo 4
# (uguale al default di base.yml, ripetuto qui come punto dello sweep).

experiment:
  name: "09_par4"
  output_dir_host: "Results/experiments"

producer:
  holdback_probability: 0.0

flink:
  parallelism: 4
